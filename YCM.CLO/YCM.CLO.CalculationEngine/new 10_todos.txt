console.log(e);

        var gridDiv = document.getElementById('grid1');
        var gridDivElements = gridDiv.getElementsByClassName('ui-grid-contents-wrapper');
        var gridLeftBodyDivElements = gridDiv.getElementsByClassName('ui-grid-render-container ng-isolate-scope ui-grid-render-container-body')
        var gridBodyDivElements = gridDiv.getElementsByClassName('ui-grid-render-container ng-isolate-scope ui-grid-render-container-body')
        //var gridLeftDiv = gridDivElements.getElementsByClassName('left ui-grid-render-container-left ui-grid-render-container');
        //var gridBodyDiv = gridDivElements.getElementsByClassName('ui-grid-render-container ng-isolate-scope ui-grid-render-container-body');

        var checkElementDivs = gridDiv.getElementsByClassName('ui-grid-row ng-scope')

       // console.log($scope.col.field);

        let cusEvent = new Event("Click");
        var ele = document.getElementById('1637847675835-0-uiGrid-06PY-cell').getElementsByClassName('ui-grid-selection-row-header-buttons ui-grid-icon-ok ng-scope');
        //ele.dispatchEvent(cusEvent);
		
		
		
		todo:
		
		*1. based on static range make custom checks
		*2. get column name from gridOptions
		**3. convert column value from string to float
		**4. get input values from UI to js controller, onchange option
		**5. make dropdown based selection for equals, not equals, range ==> put default filter text as select-filter
		6. making filter with selection options for only numeric values
		7. 